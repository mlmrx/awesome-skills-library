# Profile request

Create an AgentFacts-style profile for "DataDesk Assistant".

Available information:

- DataDesk Assistant is described as an AI agent that helps analysts summarize CSV files, draft SQL queries, and prepare short data quality notes.
- It is published by Example Analytics Lab, but no operator contact is listed.
- It accepts uploaded tabular data, natural-language analysis requests, and optional database schema notes.
- It outputs Markdown summaries, draft SQL, and data-quality checklists.
- The project page says it is "safe for enterprise use" and "does not leak data," but no audit report, privacy policy, or evaluation evidence is included.
- It does not execute SQL directly. A human analyst copies any generated SQL into their own environment.
- Known concern: users may paste customer data into prompts.
